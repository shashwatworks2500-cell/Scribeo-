export default function HomePage() {
  // Foundation only. Homepage sections (Hero, Trust, Portfolio, …) are Phase 2+
  // per the Homepage Master Specification. No placeholder UI is rendered here.
  return <>{/* TODO(Phase 2): Hero + content sections. */}</>;
}
